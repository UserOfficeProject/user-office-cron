{
  "name": "duo-cron-job",
  "version": "1.0.0",
  "description": "Node cron job service for ESS User Office project.",
  "main": "index.js",
  "repository": {
    "url": "https://gitlab.esss.lu.se/swap/duo-cron-job"
  },
  "scripts": {
    "dev": "env NODE_ENV=development env API_URL=http://localhost:4000/graphql node index.js",
    "debug": "env NODE_ENV=development env API_URL=http://localhost:4000/graphql node  --inspect index.js",
    "build": "docker build -t dmsc/duo-cron-job .",
    "prod": "env NODE_ENV=production env API_URL=http://backend:4000/graphql node index.js",
    "test": "env NODE_ENV=test env API_URL=http://localhost:4000/graphql env API_AUTH_TOKEN=testtoken jest --coverage",
    "lint": "eslint --fix . && echo Lint complete."
  },
  "lint-staged": {
    "**/*.{js,ts}": "npm run lint"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "keywords": [
    "duo",
    "cron-job",
    "microservice",
    "ess",
    "user-office"
  ],
  "author": "SWAP",
  "license": "ISC",
  "engines": {
    "node": ">=12.13.0"
  },
  "dependencies": {
    "@user-office-software/duo-logger": "^1.2.0",
    "cron": "^1.8.2",
    "dotenv": "^8.6.0",
    "envalid": "^6.0.2",
    "eslint-plugin-jest": "^23.11.0",
    "jest": "^26.6.3",
    "node-fetch": "^2.6.7",
    "sinon": "^9.0.2"
  },
  "devDependencies": {
    "eslint": "^7.0.0",
    "eslint-config-prettier": "^6.11.0",
    "eslint-plugin-prettier": "^3.1.3",
    "husky": "^4.2.5",
    "jest-fetch-mock": "^3.0.3",
    "lint-staged": "^10.1.6",
    "prettier": "^2.0.5"
  },
  "optionalDependencies": {
    "fsevents": "^2.1.3"
  }
}